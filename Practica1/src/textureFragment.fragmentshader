#version 330 core

out vec4 color;
in vec2 frag_CoordTex;
uniform sampler2D texture1;
uniform sampler2D texture2;


void main()
{
	color = mix(texture(texture1, frag_CoordTex), texture(texture2, frag_CoordTex), 0.2);
}